package student_lilija_g.homework.lesson_3.level_2;

class Task_10 {
    public static void main(String[] args) {

        /*

        String firstStr = "Hello world!";
        String secondStr = "Hello world!";
        String thirdStr = "Hello World!";

        System.out.println(firstStr.compareTo(secondStr));  // is always 0
        System.out.println(firstStr.compareTo(thirdStr));   // 32

        System.out.println(firstStr.compareToIgnoreCase(secondStr)); // always 0
        System.out.println(firstStr.compareToIgnoreCase(thirdStr));  // always 0

        System.out.println(firstStr.equals(secondStr)); // always 'true'  (String values are compared using '==', not 'equals()')
        System.out.println(firstStr.equals(thirdStr));  // always 'false' (String values are compared using '==', not 'equals()')

        System.out.println(firstStr == secondStr); // always 'true'
        System.out.println(firstStr == thirdStr);  // always 'false'

        */
    }
}
